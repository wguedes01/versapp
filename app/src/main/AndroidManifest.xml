<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.versapp" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- GCM requires a Google account. -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- Keeps the processor from sleeping when a message is received. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Creates a custom permission so only this app can receive its messages. -->
    <permission
        android:name="gandwil.who.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="gandwil.who.permission.C2D_MESSAGE" />
    <!-- This app has permission to register and receive data message. -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.NoActionBar" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".signup.SignUpNameEmailInputActivity"
            android:label="@string/title_activity_sign_up_name_email_input" >
        </activity>
        <activity
            android:name=".signup.SignUpPhoneVerificationInputActivity"
            android:label="@string/title_activity_sign_up_phone_verification_input" >
        </activity>
        <activity
            android:name=".signup.VerificationCodeInputActivity"
            android:label="@string/title_activity_verification_code_input" >
        </activity>
        <activity
            android:name=".signup.SignUpUsernamePassInputActivity"
            android:label="@string/title_activity_sign_up_username_pass_input" >
        </activity>
        <activity
            android:name=".confessions.ComposeConfessionActivity"
            android:configChanges="keyboardHidden"
            android:label="@string/title_activity_compose_confession"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".friends.FriendListActivity"
            android:label="@string/title_activity_friend_list"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>

        <receiver
            android:name=".GCMBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>

                <!-- Receives the actual messages. -->
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <!-- Receives the registration id. -->
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <category android:name="ganwill.who.gcm" />
            </intent-filter>
        </receiver>

        <service android:name=".GCMIntentService" />

        <activity
            android:name=".settings.AccountActivity"
            android:label="@string/title_activity_account" >
        </activity>
        <activity
            android:name=".chat.ChatDashboardActivity"
            android:label="@string/title_activity_chat_dashboard" >
        </activity>
        <activity
            android:name=".chat.conversation.ConversationActivity"
            android:label="@string/title_activity_conversation"
            android:parentActivityName=".chat.ChatDashboardActivity"
            android:windowSoftInputMode="stateHidden" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".chat.ChatDashboardActivity" />
        </activity>
        <activity
            android:name=".requests.RequestsActivity"
            android:label="@string/title_activity_requests" >
        </activity>
    </application>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

</manifest>
